{
  "name": "auth-service",
  "version": "1.0.0",
  "description": "Basic authentication service",
  "main": "dist/server.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf dist",
    "tsc": "tsc"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3"
  }
}
